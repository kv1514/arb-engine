{
  "mcpServers": {
    "kalshi": {
      "command": "python3",
      "args": ["-c", "from mcp_server_kalshi.server import main; main()"],
      "env": {
        "KALSHI_ENV": "${KALSHI_ENV:-demo}",
        "KALSHI_API_KEY": "${KALSHI_API_KEY}",
        "KALSHI_PRIVATE_KEY_PATH": "${KALSHI_PRIVATE_KEY_PATH}"
      }
    }
  }
}
